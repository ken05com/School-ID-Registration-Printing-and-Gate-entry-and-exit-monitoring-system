# Private MySQL data & runtime artifacts (never commit these)
database/data/
database/mysql.sock
database/mysql.pid
database/mysql.err
*.log

# Environment & secrets (each machine has its own; generated by install.php)
.env
.env.*
!.env.example

# Internal working / conversation log (not app code)
PROJECT_CONVERSATION.md

# Uploaded student photos (user-generated content)
public/uploads/students/*

# OS / editor
.DS_Store
Thumbs.db
